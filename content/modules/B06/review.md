# B06 审校记录

主任务补审：三组程序案例补齐所需头文件及 PaymentError 类型；编辑器案例补充 Document/InsertCommand 实现。显式限定对象非空、Receiver 寿命和分配成功；说明 execute 成功后历史栈分配失败仍需额外回滚设计。未使用编译器。再次运行本模块 verify.py 通过。

## 范围与版本

- 冻结考点：`B06-T01`—`B06-T11`，共 11 个；内容版本均为 `1.0.0`。
- C++ 代码基线：C++17。题目不使用依赖未指定求值次序、悬空解引用或非虚基类析构删除派生对象等未定义行为来要求“确定输出”。
- UML 记法基线：OMG UML 2.5.1。
- 本模块仅声称覆盖课程表列出的 11 个考点；未声称逐条覆盖尚未取得完整细目的官方大纲。

## 来源核验

2026-09-24 实际打开并阅读 OMG UML 2.5.1 规范索引页，核对规范版本、规范文件号 `formal/17-12-05` 与规范 PDF 链接。实际打开 C++ working draft 的 `[class.dtor]`、`[except.handle]` 页面，核对析构和异常处理规则；打开 C++ Core Guidelines 的 Classes、Resource management、Error handling、Templates 索引与规则说明；打开 cppreference virtual 页面交叉核对虚析构、构造/析构期间分派；打开设计模式目录用于交叉核对分类。设计模式目录是二手资料，未当作标准。

## 正文核验

逐篇检查八个必需标题、完整推导例、边界条件、反例和出处。C++ 三篇分别人工推演构造/析构逆序、异常展开、虚调用、对象切片、模板实例化与 `vector` 失效；UML 三篇检查箭头方向、菱形端、多重性、include/extend、alt、fork/join；模式三篇检查意图、变化点和参与者职责；两篇综合正文检查跨图一致性与 C++ 所有权。

## 练习核验

- 总题量 74：T01=4，T02=8，T03=8，T04=6，T05=8，T06=8，T07=5，T08=5，T09=6，T10=8，T11=8。
- 选择题逐项检查四个选项和四条针对性解析；主观题逐项检查答案包含实际关系、次序、复杂条件或接口，而非“过程正确”等空泛表述。
- UML 综合案例 8 组、C++ 模式综合案例 8 组。案例材料只包含业务事实/约束，不包含待求关系、输出或模式答案；子问题与顶层答案分离。
- 人工复算锚点：析构顺序为 `D 析构体 → d → B 析构体 → b`；限定虚调用输出为 `D, B`；构造期基类虚调用落到 `Base::f`；异常处理器按从前到后匹配；强保证案例先写临时缓存、成功后提交。
- T11 前三题含完整多角色 C++17 程序骨架和三处填空。人工推演结果分别为支付 `card:3000`/`failed:limit`、装饰链 `E(Z(F(x)))`、编辑器成功/撤销/越界失败后的 `(abX,1)/(ab,0)/(ab,0)`。
- `verify.py` 独立检查注册数量、题量、正文标题与长度、选择题解析、评分点、案例答案泄漏、案例数量和 SVG 引用；另以 Python 支付模型、函数组合模型、Document/History 状态机从输入重新计算上述三组状态，不读取答案文本作为“执行”证据。

## 图形资产

`assets/course-enrollment-blank.svg` 为本模块原创待补 SVG，与 T10-Q01 的 Course、Chapter、Student、Enrollment 材料一致。图中关系类型、菱形位置和多重性均以空白或问号保留，未提前泄露答案；题干明确一门可发布课程至少一章、章节不可独立存在、学生与课程均可零到多。

## 验证限制

本机 `PATH` 暂未发现 C++ 编译器。本轮没有编译或执行 C++ 片段；验证依据为 C++17 标准语义手工推演、规范/技术资料交叉核对及独立结构/语义断言。不得把该核验描述为编译通过。所有练习均为原创教学题，不标作真题或回忆题。
