# 002 验证记录

日期：2026-09-24。状态：新版可试用，学习效果与难度标定等待用户验证。

## 内容与研究

当前内容：SE-01 计算机系统与性能计算、SE-02 数据结构与算法分析、SE-03 数据库与关系建模；每单元四小节、八题，总计 12 小节、24 题（15 选择、6 简答、3 代码填空）。原 18 题归档。

研究范围与可信度见 docs/exam-difficulty-review.md。采用能实际读到题干的 2022、2023 历史样本与出版社目录；排除只有预告的年份页面。未宣称获取全部最新试卷、官方答案或统计难度。

## 实测

- `python -m pytest tests -q --basetemp=data/pytest-exam-03`：24 passed，0.96s。
- `node --check static/app.js`：通过。
- 计算核验：补码、溢出、Cache 期望、芯片数量、流水时间、地址拆分、Amdahl。
- 算法核验：栈序列模拟、遍历重建；二分的空数组/单元素/成功/失败路径；连续子数组模型对 363 组数组与穷举结果一致。
- SQL：代码填空和两段课程查询在 SQLite 中得到预期结果；验证 LEFT JOIN 的 COUNT 列与 COUNT(*) 差别。
- 兼容：恢复原版 schema 1 备份后，旧答案和笔记仍可读取；旧题不计入新版统计；新版会话从 SE-01 开始；schema 2 包含新旧内容并可通过恢复校验。
- 浏览器：10 分钟开始新小节，读完进入对应题；选择题正确显示绿色 ✔，错误显示红色 ❌，结果自动滚入视口；简答提交为灰色待自评，自评后显示绿色 ✔；回顾页符号与文字一致。
- 知识单元页：新三专题、后续待制作路线、旧版选读归档均可见；展开归档后课程和题库入口可见。浏览器错误日志为空。

浏览器测试使用 data/qa-exam-v2.sqlite3 与 8766 端口。生产端口仍为 8765。升级前完整备份为 data/backups/before-curriculum-v2-20260924-141521.json。

生产服务重新加载后，API 确认版本 2.0.0、3 单元、24 题；对比升级前备份，6 条原作答逐条保留，笔记与反馈内容一致。已在生产知识单元页确认新版可见，测试服务已关闭。

本机未发现可用 C 编译器；C 参考代码做了人工逐行审查，算法结论通过独立 Python 模型与边界测试验证，不能将此称为 C 编译运行测试。用户输入依然不在线执行。

## 自审修订

- 明确 Cache 未命中“额外时间”与“总时间”的区别，流水线首条延迟模型在题干写清。
- 动态规划明确非空连续区间、n≥1 和整数不溢出的前提。
- SQL 和关系键以业务粒度为依据，不机械套用多对多二列键。
- 新旧内容采用不同 ID，避免将旧题的正确记录误算为新题已掌握。
- 未自评不标红，自评结果明确为学习判断。
