# 003 实施验收记录

执行日期：2026-09-24。状态：知识库与平台接入完成；真题正文库、官方完整考纲审定未完成。未提交 Git。

## 内容交付

| 板块 | 考点 | 新增练习 |
| --- | ---: | ---: |
| B01 计算机系统与操作系统 | 10 | 83 |
| B02 程序语言与编译基础 | 8 | 48 |
| B03 数据结构与算法 | 12 | 84 |
| B04 数据库与数据建模 | 10 | 67 |
| B05 软件工程与结构化分析 | 11 | 86 |
| B06 面向对象、UML 与 C++ | 11 | 74 |
| B07 网络与信息安全 | 10 | 66 |
| B08 数学与综合基础 | 9 | 56 |
| 合计 | 81 | 564 |

原 SE 24 题仍可使用，因此默认题库共 588 题；CSF 18 题另作选读归档。新增题均为独立编写的练习，不能算成真题。28 组共享材料案例：DFD 3、数据库 3、算法 6、UML 8、C++ 模式 8。

内容规范、板块检索规则及模板位于 docs/content-guide；模型与生成的 JSON Schema 保持一致。每个知识点有单独 Markdown 与 JSON，每题独立 JSON；来源、答案来源、版本、题量理由与审校证据可追溯。详细章节映射及结构缺口见 content/generated/coverage.json。

## 已执行检查

- `python scripts/build_content.py`：8 板块、81 考点、564 题、84 份试卷元数据；结构 errors=[]。检查 ID、必需章节、文件路径、来源、归属、练习目标、先修无环与五类案例数量。
- B01—B08 的 `verify.py` 全部退出码 0。包括补码/调度/RAID、词法/LL(1)/传参模型、树重建/Huffman/动态规划、依赖闭包/无损分解/事务优先图/SQL、覆盖率/PERT/DFD、模式状态模型、CIDR/TCP/RSA、概率/运筹/容量计算等。
- `python -m pytest tests -q --basetemp=data/pytest-knowledge-final-02`：37 passed。
- `node --check static/app.js`：退出码 0。
- `git diff --check`：退出码 0；仅提示仓库的 LF/CRLF 转换规则。

回归覆盖：目录按需加载、正文搜索、组合筛选、分页、列表与未作答详情不含解析、子问题禁止夹带答案、待核验题拒绝判分、案例自评、试卷原序、附件不能读答案 JSON；v1/v2/v3 兼容、旧 v3 内容子集恢复、损坏备份拒绝且状态不变。内容故障测试证实先修环、失效来源、空答案、空完整卷和数组题文件会阻止发布。

## 浏览器验收

使用 `data/qa-knowledge.sqlite3`、端口 8766，实际浏览器交互核验：八板块导航、考点列表与关键词搜索；从正文跳转练习时正确选中考点；选择题独立作答后显示判分和逐项解释；主观草稿刷新恢复；案例共享材料、子问和自评保存；2005/H1/应用技术组合筛选及逐卷来源定位；真题页明确显示 0 正文与 0 完整卷。

浏览器发现的 UML 图文不符已修复为 Course/Chapter/Student/Enrollment 待补图，并补齐数量约束。桌面截图复核了知识库导航、列表及阅读布局。浏览器 QA 不写入用户数据库。

最终浏览器复查确认新课程类图实际渲染、共享材料与子问题对应；控制台 error 日志为空。现有 8765 学习服务已重启并返回 v3 内容、588 道默认练习。升级前备份为 `data/backups/before-knowledge-v3-20260924-154654.json`，与升级后 SQLite 原始 state 逐字段比较完全一致；API 对失效会话只改变展示，不删除已保存记录。

## 实质审查与修订

- 将多板块的通用评分话术替换为逐题结果、条件、错误项理由和可自评要点。
- 修正二叉树左右子树判断、英语代词指代和软件合法复制品修改权的条件表述。
- 算法案例删除已解结果，改为输入和约束，保留完整状态表、复杂度与反例。
- C++ 前三模式案例加入程序填空与独立状态模型。主任务补齐头文件、异常类型和具体命令实现；明确非空对象、寿命与内存分配成功假设，未将有限异常示例描述为普遍强异常保证。
- 补齐 RAID/NAS/SAN、编译文法、数据库访问/NoSQL、成熟度/历史质量标准、Web/MVC/系统迁移、对象状态测试、信息化与云计算。

## 未完成与验证边界

1. **真题正文 0 题，完整收录 0 卷，真题答案核验 0 题。** 完成的是 2005—2026 的 88 条场次矩阵和 84 个逐卷来源定位；2020 H1 的延期场次与 2026 H2 尚未举行分别登记。不能用目录数替代真题完成度。早年原卷、机考多批次和 Java/C++ 原题的实际收录仍待补齐。
2. 原始来源未提供足以确认本项目批量收录的依据，按批准方案保留链接与缺口，不抄录授权不明商业全库。实际检索和页面差异见 content/exams/review.md；取得可用文件后可直接按既定 Schema 入库。
3. 官方大纲完整正文尚未取得。已核实出版社 2018 大纲版本；30 个章节映射基于明确披露的第三方转录，不宣称全纲逐条审定或所有细目深度已覆盖。
4. C/C++ 未在编译器执行；使用规范资料、手工推演及独立 Python 模型核验。SQL 用 SQLite 实测可移植子集；未部署 PostgreSQL/MongoDB 验证产品特性。机器校验不能代替所有文字内容的专业人工复审。
5. v3 恢复支持当前内容及只增补内容后的未修改旧子集；任意改写过题意的外部快照会被拒绝，不自动导入未知内容覆盖当前库。
