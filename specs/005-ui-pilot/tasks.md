# 005 实施任务

1. 建立共享响应式外壳和可访问导航；验证断点、焦点返回、跳过链接与保存重试。
2. 整理首页决策流和任务预览；验证三模式、防竞态逻辑及当前 session 完成状态。
3. 整理学习页阅读层级和目录；验证 hash 路由、section index、正文内容不变。
4. 新增数组二分纯函数与 `SE-02/2` 图解，并在 `B03-T09` 加补充入口；用 Node 定向测试成功、失败和重置路径。
5. 运行 JavaScript 语法检查、定向测试和全量 Python 回归；使用隔离数据库完成首页、学习页和图解交互检查。
6. 在同一冻结版本上完成 1440、1024、768、640、390 像素截图，并核对 719/720、1099/1100 断点；对练习、真题和回顾页做烟雾检查。
7. 固定版本交由独立审查，清零阻断问题后交用户试用；收集反馈后再决定是否推广到其他业务页。
