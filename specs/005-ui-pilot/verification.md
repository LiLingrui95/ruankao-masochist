# 005 验证与审查记录

日期：2026-09-24。

## 自动检查

- 基线回归：45 passed（8.46s）。
- 最终实现回归：45 passed（7.03s），`--basetemp` 位于 `D:\study\data`。
- `node --check static/app.js` 与 `node --check static/binary-search.js` 通过。
- `node tests/test_binary_search_diagram.js` 通过：成功路径比较 28 → 47 并返回下标 5；失败路径比较 28 → 47 → 36，最终 `low=4`、`high=3`、无 `mid` 并返回 -1。
- `git diff --check` 无空白错误；仅提示现有 Windows CRLF 转换。

## 浏览器交互

- 二分图解的目标切换、前进、后退、重置和键盘操作通过；首尾按钮状态正确。
- 图解前后 `/api/backup` 的 `state` 完全一致，未写入阅读、作答或其他学习状态。
- Escape 关闭手机菜单并把焦点还给菜单按钮；跳过链接聚焦 `main` 且保持当前 hash。
- 390px 下练习、真题、回顾页烟雾检查通过，页面内容宽度 375px，小于 390px viewport。
- 390px 学习页最终复测：`documentElement.scrollWidth=375`；`.learn-layout`、直接子 `div` 与 `.reading-card` 均为 345px；二分数组为 283px，七列各约 37.85px。
- 查找 35 的第 6/8 步中 `low=mid=high=4` 使用三行 10px 标签，无重叠且可辨认。
- 离线服务 → 输入笔记 → 显示“待重试” → 服务恢复后点击保存 → 显示“已保存” → 刷新，笔记仍保留。

## 独立审查

本试点由 GPT-5.6 Sol 实施，GPT-6 Astra 独立复审。冻结版审查发现并已修复：手机端保存状态仅显示色点；同格二分指针标签重叠；首页开始按钮位于预览之前。其余业务逻辑通过审查。

窄屏首轮修复只约束 Grid 子项，仍由 `1fr` track 的自动最小值把文章撑到 621px。最终将手机单列改为 `minmax(0, 1fr)`；复测确认整页横向溢出消失。代码块和表格继续只在各自容器内部滚动。

390px 与 640px 的学习页溢出复测通过。768px 曾测得 `scrollWidth=850`、`innerWidth=768`；暂停同方向修正并向用户说明后，用户授权采用 `<1100px` 统一单列方案：学习 track 使用 `minmax(0, 1fr)`，目录回到正文之后。复测 768px 为 `scrollWidth=753`、正文 track 515px，且目录顶部 2610px 大于文章底部 2283px。1024px 为 1009px、1440px 为 1425px，均未整页溢出。

边界复测：719/720/1099/1100 的 `scrollWidth` 分别为 704/705/1084/1085；学习 track 分别为 674/467/846px，1100px 恢复 501px + 250px 双列。菜单只在 719px 显示；侧栏在 720–1099px 为 188px，1100px 为 248px。

图解快捷入口已设置 `scroll-margin-top`（手机 76px，其余 86px），避免 sticky topbar 遮挡图解眉题与标题；该项独立于 Grid 阻断。

隔离数据库功能复测：自测默认展示 3 项、可展开其余 2 项；只预览错题不会把当前 active mode 从 test 改为 mistakes；选择题草稿刷新后保留；错题重做正确显示绿色完成态；简答提交先显示灰色“待自评”，选择“未满足”保存后显示红色“自评待巩固”。

最终截图：首页 390/640/768/1024/1440 的 `scrollWidth` 分别为 375/625/753/1009/1425；390/640 图解页分别为 375/625，快捷入口跳转后眉题与标题完整可见。练习、真题、回顾在 1440px 均为 1425；其 390px 烟雾检查均为 375。后三页只验证共享外壳与无整页溢出，未进行本阶段的业务页视觉改造。

增量审查发现 Grid 收缩规则曾把正文 760px 上限覆盖为 100%。修复后，1920px 下正文为 760px、页面 `scrollWidth=1905`；390px 下正文为 305px、页面 `scrollWidth=375`。正文继续遵守 `max-width: min(760px, 100%)`，该一般回归已修复。

正文色 `#17302d` 在白底对比度 14.03:1，强调色 `#087b70` 在白底对比度 5.15:1。

生产备份的 `state` 与 16:28 基线逐项相等，保留 6 条作答；`app.py` SHA 与基线相同。验证未修改生产学习记录、后端 API 或数据库格式。

## 范围结论

本阶段只完成首页与学习页试点。练习、真题、回顾仅做共享外壳烟雾检查；是否推广视觉与交互改造，等待用户试用反馈后决定。

两页试点及约定的多尺寸、边界、状态和烟雾检查均已完成。是否推广到其他业务页，等待用户试用反馈后决定。
